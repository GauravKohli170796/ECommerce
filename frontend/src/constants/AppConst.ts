export const AppConst = Object.freeze({
   BackendURL: "http://localhost:4000/",
   productsPerPage:10
});

export enum drawerShowOptions {
   filter = "filter",
   search = "search",
   default= "nothing"
}